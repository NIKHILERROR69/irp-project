#include<stdio.h>
void main()
{
int num,i=0;
printf("enter a number to print the tables");
scanf("%d",&num);

printf("the tables for the numbers as follows\n");
         while(i<=100)
         {
         ++i;
         printf("%d X %d = %d\n", num,i,num*i);
         
         }











}